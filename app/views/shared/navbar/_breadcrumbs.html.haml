.breadcrumb-container.span5.pull-right
  %ul.breadcrumb
    %li
      =link_to image_tag("home.png"), customers_customer_index_path()
    - if @breadcrumbs
      -@breadcrumbs.each do |txt, path|
        %li
          %span.divider= ">"
          %span
            =raw(path)
  = render "shared/navbar/breadcrumbs_profile"

