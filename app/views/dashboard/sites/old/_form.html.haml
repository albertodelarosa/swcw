= form_for @dashboard_site do |f|
  - if @dashboard_site.errors.any?
    #error_explanation
      %h2= "#{pluralize(@dashboard_site.errors.count, "error")} prohibited this dashboard_site from being saved:"
      %ul
        - @dashboard_site.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
