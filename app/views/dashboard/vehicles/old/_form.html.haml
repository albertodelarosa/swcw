= form_for @dashboard_vehicle do |f|
  - if @dashboard_vehicle.errors.any?
    #error_explanation
      %h2= "#{pluralize(@dashboard_vehicle.errors.count, "error")} prohibited this dashboard_vehicle from being saved:"
      %ul
        - @dashboard_vehicle.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
