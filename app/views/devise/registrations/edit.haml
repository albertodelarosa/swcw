.col-xs-12.col-sm-4
  .well.well-default.text-center
    %h3
      Unhappy?
    %h3
      #{button_to "Cancel my account", registration_path(resource_name), :data => { :confirm => "Are you sure?" }, :method => :delete, class: "btn btn-danger btn-outline btn-block"}
.col-xs-12.col-sm-8
  = render "form"

