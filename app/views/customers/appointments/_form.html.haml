= form_for @customers_appointment do |f|
  - if @customers_appointment.errors.any?
    #error_explanation
      %h2= "#{pluralize(@customers_appointment.errors.count, "error")} prohibited this customers_appointment from being saved:"
      %ul
        - @customers_appointment.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
